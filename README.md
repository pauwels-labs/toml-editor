# toml-editor
Tiny container backed by the rust base image and with toml-cli installed.

Used as a step in CI/CD pipelines to easily edit the Cargo.toml, namely to update the version string before upload to crates.io.
